services:
  - type: web
    name: contact-lenses
    runtime: static
    buildCommand: mkdir -p public && cp index.html app.js styles.css products.json target_brands.json common-products.json public/
    staticPublishPath: public
